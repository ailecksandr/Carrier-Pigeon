- content_for :scripts do
  = include_javascript 'plugins/flash'

- flash.each do |key, value|
  - unless value.blank?
    %div{ class: flash_class(key)}
      %button.close{ data: {dismiss: 'alert'}, type: :button }
        %span{ aria: { hidden: true } } ×
        %span.sr-only Close
      = errors_list(value)